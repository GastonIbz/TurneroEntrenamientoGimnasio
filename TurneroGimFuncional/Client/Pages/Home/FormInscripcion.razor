<h3>FormInscripcion</h3>


</EditForm>

@code {
    [Parameter] public Inscripcion inscripcion { get; set; } 

        <label>  Id del Entrenamiento </label>
 
        <div>
          <InputText class="form-control" @bind-Value="@inscripcion.EntrenamientoId"/>
        </div>
    </div>

           <div class="form-group"> 

        <label>  Fecha del Turno </label>
 
        <div>
          <InputText class="form-control" @bind-Value="@inscripcion.FechaTurno"/>
        </div>
    </div>

    <button class="btn btn-primary" type="submit"> Aceptar </button>
    <button class="btn btn-secondary" @onclick="onCancel"> Cancelar </button>


</EditForm>
@code {
    [Parameter] public Inscripcion inscripcion { get; set; }
    [Parameter] public EventCallback onValidSubmit { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
}
